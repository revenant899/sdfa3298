@model IEnumerable<Category>
@{
    ViewData["Title"] = "Категорії";
}

@if (Model.Count() > 0)
{
    <div class="d-flex">
        <h1 class="flex-grow-1">Категорії</h1>
        <div class="flex-grow-1 text-end">
            <a class="btn btn-dark" asp-controller="Category" asp-action="Create">Додати категорію</a>
        </div>
    </div>
    <div>
        <table class="table table-striped w-75 m-auto">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col" class="text-center">Name</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr>
                        <td>@category.Id</td>
                        <td class="text-center">@category.Name</td>
                        <td class="text-end">
                            <a asp-action="Update" asp-route-id="@category.Id" class="btn btn-success m-1">Редагувати</a>
                            <button onclick="deleteCategory('@System.Text.Json.JsonSerializer.Serialize(category)')" data-bs-toggle="modal" data-bs-target="#deleteModal" class="btn btn-danger m-1">Видалити</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <h1 class="text-center">Категорії не знайдено</h1>
}

